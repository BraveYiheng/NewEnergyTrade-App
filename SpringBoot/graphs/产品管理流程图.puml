@startuml
title 产品管理模块流程
start
:供应商选择操作（添加/删除/查看产品信息）;
if (添加产品) then (yes)
    :填写产品信息;
    :验证信息合理性（价格非负、库存非负等）;
    if (信息不合理) then (yes)
        :返回错误提示;
    else (no)
        :信息合理，存储产品信息并关联供应商ID;
        :返回添加成功消息;
    endif
else if (删除产品) then (yes)
    :选择要删除的产品;
    :发送删除请求及产品ID;
    :删除产品记录并处理关联订单数据;
    :返回删除成功消息;
else if (查看产品信息) then (yes)
    :构建查询条件（分类、筛选等）;
    :发送到系统;
    :系统查询产品数据;
    :返回数据展示产品信息;
endif
stop
start
:采购商选择操作（搜索产品、下单）;
if (搜索产品) then (yes)
    :构建搜索条件;
    :发送到系统;
    :系统查询产品数据;
    :返回数据展示产品列表;
else if (下单) then (yes)
    :基于展示的产品列表选择产品下单;
    :生成订单数据;
    :发送订单数据到系统;
    :系统检查库存;
    if (库存不足) then (yes)
        :返回错误提示;
    else (no)
        :库存充足，插入订单记录并更新库存;
        :返回下单成功消息;
    endif
endif
stop
@enduml